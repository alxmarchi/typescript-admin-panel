 import { ChartValue } from "../models/objects/ChartValue";
import {QueryResult} from "../models/objects/QueryResult"

// type setTargetDataType = (targetData : Array<ChartValue>) => Array<ChartValue>;

// type cleanerDataType = (rawData: Array<QueryResult>, targetFilter : Array<string>, setTargetData: setTargetDataType) => Array<ChartValue>;

// export const makeCleanDataFromFetchResponce : cleanerDataType = (rawDataFromResponce, target, setTargetData) => {


//  const targetFilteredData = rawDataFromResponce.filter(function (serie: QueryResult)  {
//    return target.indexOf(serie.target) > -1
//   } );

//     return setTargetData(targetFilteredData);

   

// }

const targetMapping = () => {
    
}